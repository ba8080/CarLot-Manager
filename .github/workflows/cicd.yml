name: Full CI/CD Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore

      - name: Run Tests
        run: dotnet test --no-build --verbosity normal

  docker_build_push:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/carlot-manager:latest -f CarLot-Manager.API/Dockerfile .

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/carlot-manager:latest

  terraform:
    name: Deploy Infrastructure (Terraform)
    runs-on: ubuntu-latest
    needs: docker_build_push
    defaults:
      run:
        working-directory: infra/

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: eu-central-1

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan

      - name: Output ALB DNS
        id: infra
        run: |
          alb_dns=$(terraform output -raw alb_dns)
          echo "alb_dns=$alb_dns" >> $GITHUB_OUTPUT
          echo "ALB DNS: $alb_dns"

      - name: Create Deployment URL File
        run: echo "http://${{ steps.infra.outputs.alb_dns }}" > deployment_url.txt

      - name: Upload Deployment URL
        uses: actions/upload-artifact@v4
        with:
          name: deployment-url
          path: deployment_url.txt

  notify:
    needs: terraform
    runs-on: ubuntu-latest
    steps:
      - name: Download Deployment URL
        uses: actions/download-artifact@v4
        with:
          name: deployment-url

      - name: Print Deployment URL
        run: |
          echo "=============================="
          echo "🚀 Application Deployed!"
          echo "URL:"
          cat deployment_url.txt
          echo "=============================="

      - name: Check Application Health
        run: |
          URL=$(cat deployment_url.txt)
          echo "Checking health at: $URL"
          
          # Retry until healthy or fail after timeout
          for i in {1..10}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$URL")
            
            if [ "$STATUS" = "200" ]; then
              echo "✅ Application is healthy! (HTTP 200)"
              exit 0
            fi
            
            echo "Status $STATUS - retrying in 5 seconds..."
            sleep 5
          done
          
          echo "❌ Application did NOT return 200 within timeout."
          exit 1
