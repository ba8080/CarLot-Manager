apiVersion: v1
kind: Service
metadata:
  name: {{ include "carlot-app.fullname" . }}
  labels:
    app: {{ include "carlot-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "carlot-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
